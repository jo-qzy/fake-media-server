INCLUDE_DIRECTORIES(.)

AUX_SOURCE_DIRECTORY(common COMMON_SOURCE_FILES)
ADD_LIBRARY(FUTIL OBJECT ${COMMON_SOURCE_FILES})

AUX_SOURCE_DIRECTORY(. SOURCE_FILES)
ADD_LIBRARY(FMEDIA OBJECT ${SOURCE_FILES} ${COMMON_SOURCE_FILES})
