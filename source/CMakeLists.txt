INCLUDE_DIRECTORIES(.)

AUX_SOURCE_DIRECTORY(util UTIL_SOURCE_FILES)
ADD_LIBRARY(FUTIL OBJECT ${UTIL_SOURCE_FILES})

AUX_SOURCE_DIRECTORY(. SOURCE_FILES)
ADD_LIBRARY(FMEDIA OBJECT ${SOURCE_FILES} $<TARGET_OBJECTS:FUTIL>)
