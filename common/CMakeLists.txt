INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/common)

AUX_SOURCE_DIRECTORY(util UTIL_SOURCE_FILES)
AUX_SOURCE_DIRECTORY(net NET_SOURCE_FILES)
ADD_LIBRARY(FCOMMON OBJECT
        ${NET_SOURCE_FILES}
        ${UTIL_SOURCE_FILES})